post_install() {
  fc-cache -fs /usr/share/fonts/otf-yanone-kaffeesatz-ibx >/dev/null
  echo "Done."
}

post_upgrade() {
  post_install $1
}

post_remove() {
  fc-cache -sf > /dev/null
  echo "Done."
}

op=$1
shift
$op $*

