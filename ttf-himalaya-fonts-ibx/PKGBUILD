# Maintainer: bohoomil <@zoho.com>

pkgname=ttf-himalaya-fonts-ibx
pkgver=1.0
pkgrel=4
depends=('fontconfig')
makedepends=('unrar')
pkgdesc="Himalaya fonts."
url="http://www.tibetebook.com/"
arch=('any')
groups=('infinality-bundle-fonts-extra')
license=('Custom')
noextract=('HimalayaFonts.rar')
install=$pkgname.install
source=(http://www.tibetebook.com/nyer_mkho/demig/HimalayaFonts.rar)
sha1sums=('10cf2adab4d9d64807dfc9455bb1949acdc41cff')

prepare(){
  cd ${srcdir}
  unrar e HimalayaFonts.rar
}

package(){
  cd "${srcdir}"

  install -m755 -d "${pkgdir}"/usr/share/fonts/"${pkgname}"
  install -m644 *.ttf "${pkgdir}"/usr/share/fonts/"${pkgname}"
}
